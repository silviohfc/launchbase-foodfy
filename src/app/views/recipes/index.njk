{% extends "layout.njk" %}

{% block content %}

    <section id="topRecipes">
        <div id="topRecipesGrid">

            {% for recipe in recipes %}
                <div class="recipeCard" id="{{loop.index}}">
                    <div class="img" style="background-image: url({{ recipe.image }});"></div>
                    <h4>{{recipe.title}}</h4>
                    <p>{{recipe.author}}</p>
                </div>
            {% endfor %}

        </div>
    </section>

    <script src="/scripts.js"></script>
{% endblock %}
   